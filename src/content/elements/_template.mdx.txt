<!--
  elements カードのテンプレート。

  このファイルは拡張子を .mdx.txt にしてあるため、
  content.config.ts の glob パターン（**/*.{md,mdx}）にはマッチしない
  ＝ビルド対象・コレクション対象にはならない。

  新しいカードを作るときは、この内容をコピーして
  拡張子 .mdx の新規ファイル（slug ベースのファイル名）として保存すること。

  【引用ルール（重要）】
  1カードあたりの歌詞引用は、分析に必要な最小フレーズ（1〜2行以内）に限定する。
  歌詞全文は repo に置かない。既存 CLAUDE.md「Public repo ガードレール」を
  elements 配下にも同様に適用する。

  【seed と作業ノートの分離（重要）】
  カード本文には「観察」だけを書く。
  裏取り控え（流通歌詞との突き合わせ、メタデータ確認等）は「作業ノート」であり
  repo に書かない（Obsidian / iPhone メモに退避する）。
  書誌情報は sources frontmatter に置けば十分で、本文で重複させない。
-->
---
title: ""            # カード名（例: "k→t 子音ピボット（〇〇型）"）
type: ""              # 推奨語彙: pivot | sequenz | rhyme | syllable | prosody | other
                       # （z.string() なので上記以外も書けるが、10〜20枚溜まってから enum 化を検討する）
maturity: seed        # seed | budding | evergreen
tags:
  phoneme: []          # 関与する音素・音素列。romaji 小文字、正規化ルールは CLAUDE.md 参照（例: [k, a]）
  repetition:          # 反復の類型。c=子音のみ一致 / v=母音のみ一致 / cv=両方一致
  artist: []           # アーティスト（romaji slug）
  era: ""              # 年代（例: "2010s"）
  lang: ja             # ja | en | fr | ...
related: []            # 関連カード・essay の slug（双方向リンクの素）
sources: []            # 出典（曲名・作詞者・年。全文引用はしない。未確認のまま記載しない）
# figure（任意）— X に出るサムネイル画像に描く図。書かなければタイトルのみの絵になる。
# 対称形のカードなら下の3行を有効にする。いまは kind: symmetry だけが使える。
# units は最大8音まで（それ以上は歌詞の再現に近づくため、ビルドが止まる）。
# highlight は強調する units の位置（0始まり）で、指定が2つ以上あると弧で結ばれる。
# figure:
#   kind: symmetry
#   units: ["し", "て", "し"]
#   highlight: [0, 2]
created: YYYY-MM-DD
updated: YYYY-MM-DD
---

## 観察（YYYY年M月D日）

（歌詞引用は最小フレーズ1〜2行以内）

（観察メモ）
